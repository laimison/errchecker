tail --follow=name --retry /tmp/tomcat-logs/* doesn't check for new files, also breaks if binary is added to this directory

- Mention that this monitors only Exceptions and excludes errors
- maven-jar-plugin needed to not create jar
